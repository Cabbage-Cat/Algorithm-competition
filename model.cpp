#include <bits/stdc++.h>
#define _for(i,a,b) for (int i=(a);i<(b);i++)
#define _rep(i,a,b) for (int i=(a);i<=(b);i++)

int readint(){
    int x; scanf("%d",&x); return x;
}
using namespace std;

int main(){
    return 0;
}